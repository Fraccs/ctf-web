name: test-e2e

on: pull_request

jobs:
  test-e2e:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Test
      uses: cypress-io/github-action@v5
      with:
        build: npm run build
        start: npm start
        browser: chrome
